/build
/builddir
/.vscode
*.so
__pycache__
tmp
*.log
*.xml
test_results.txt
artifacts
cprofile
*.prof
.venv
**/.venv

# Per-framework scripts emitted by the qa/common model generation drivers
qa/common/gen.*.sh
qa/common/gen.*.cmds

# Test exclusions
qa/L0_openai/openai

# L0_backend_python_pytest: generated model repos, server logs, cloned
# backends, and JUnit XML all land under each scenario's output/ so a run
# can be cleaned up with one `rm -rf` without touching source.
qa/L0_backend_python_pytest/*/output/
tensorrtllm_models
tensorrtllm_mistral_models/
custom_tokenizer
